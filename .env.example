# Delivery. Keep mock for local fixture runs; use webhook for real delivery.
DELIVERY_ADAPTER=mock
DELIVERY_WEBHOOK_URL=

# Optional generic JSON model analyzer (set adapters.analyzer: http-json in YAML).
MODEL_API_URL=
MODEL_API_KEY=
MODEL_NAME=
