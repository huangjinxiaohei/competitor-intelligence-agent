# ?????? Agent

?????????????????????????????????????? Agent?????????????????????????????????????????????

## ??

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -e ".[test,browser]"
python -m playwright install chromium
Copy-Item .env.example .env
```

`.env` ?? CLI/???????????? Git ?????????????????

## ??

```powershell
competitor-agent doctor --fixture
competitor-agent discover --fixture --dry-run
competitor-agent run --fixture
competitor-agent report RUN_ID
competitor-agent push-test --fixture
```

- `doctor` ??????????????
- ??? `--dry-run` ?????????????????????????
- ?? `--dry-run` ?????????????
- `push-test` ?? mock ?????????????????????

## ??????

`fixtures/manifest.json` ?????????

1. `round_01_baseline`?????? configuration ??????
2. `round_02_changed`?????? configuration ???????????????
3. `round_03_changed`???????????????????????

???????

```powershell
competitor-agent run --fixture
competitor-agent run --fixture
competitor-agent run --fixture
```

SQLite ????? `state/competitive_intel.db`?Markdown/JSON/CSV ????? `reports/YYYY-MM-DD/`?

## ?????? Agent ??

?? `config/project.yaml` ??????????? `seed_urls`??? Agent ???????? `state/host_search_results.json`???????????????????????

???? JSON ???

```json
[
  {"title": "Candidate", "url": "https://candidate.example", "snippet": "agent platform pricing"}
]
```

???????

```json
{
  "agent platform": [
    {"title": "Candidate", "url": "https://candidate.example", "snippet": "agent platform pricing"}
  ]
}
```

???? `competitor-agent discover` ? `competitor-agent run`???????????????????????? PDF ????

## ?????????

?? `adapters.analyzer: heuristic` ?????????? JSON ??????? YAML ?? `http-json` ?? `.env` ???

```dotenv
MODEL_API_URL=https://model-endpoint.example/v1/extract
MODEL_API_KEY=...
MODEL_NAME=...
```

?????????? `schemas/ProductSnapshot.schema.json`??????????????????????????? Evidence?

????????????

```dotenv
DELIVERY_ADAPTER=webhook
DELIVERY_WEBHOOK_URL=https://collaboration-webhook.example/...
```

?????????? `DeliveryReceipt`???????????Webhook ?????? 5 ??????

## ????

?????? 09:00????? `-Apply` ?????

```powershell
.\scripts\install_schedule.ps1
.\scripts\install_schedule.ps1 -Apply
.\scripts\uninstall_schedule.ps1
.\scripts\uninstall_schedule.ps1 -Apply
```

Windows ????????????????? `Asia/Shanghai`??????????? `agent/contract.md`?
