# Extract task

Input: one `SourceDocument`. Output one `ProductSnapshot` conforming to `../../schemas/ProductSnapshot.schema.json`. Every factual field must have compact, directly attributable `Evidence`; keep uncertainty in `confidence`, never invent unavailable fields.
