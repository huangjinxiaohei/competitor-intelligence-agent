# Digest prompt

Given validated change events, produce a concise Chinese digest compatible with `schemas/Digest.schema.json`. Lead with the business impact, rank high-importance changes first, state uncertainty, list failed sources, and keep each change linked to its evidence.
