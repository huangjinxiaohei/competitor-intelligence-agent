# Extraction prompt

Extract only facts visible in the source. Return JSON compatible with `schemas/ProductSnapshot.schema.json`. Cite every structured claim with `Evidence` using the source URL, a short exact excerpt, and observation time. Preserve pricing qualifiers, missing values, and ambiguity.
